"""
LLM Router module.

Handles LLM provider selection, fallback chains, and rate limiting.
Supports Gemini (primary) and Groq (fallback).
"""

# LLM routing functionality is currently in the orchestrator
# This module is prepared for future extraction
