# =====================================================================
# NL2SQL Multi-Agent System - Environment Configuration
# =====================================================================
#
# SECURITY WARNING:
# -----------------
# 1. NEVER commit this file with real API keys to Git!
# 2. The .gitignore is configured to ignore .env files.
# 3. Copy this file to `.env` before editing.
#
# SETUP INSTRUCTIONS:
# -------------------
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and add your API key
# 3. Run: python setup.py
# 4. Then: python cli.py
#
# GET API KEYS:
# -------------
# - Groq (free tier): https://console.groq.com/keys
# - Google Gemini: https://makersuite.google.com/app/apikey
#
# =====================================================================

# =====================================================================
# LLM PROVIDER CONFIGURATION
# =====================================================================

# Option 1: Groq (RECOMMENDED - Fast inference, free tier available)
# Get your key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Option 2: Google Gemini (Alternative - also has free tier)
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=AIzaSy_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Which provider to use (groq or gemini)
LLM_PROVIDER=groq

# Model selection (recommended defaults)
# - Groq: groq/llama-3.1-70b-versatile (131K context, fast)
# - Gemini: gemini/gemini-pro
LLM_MODEL=groq/llama-3.1-70b-versatile

# =====================================================================
# DATABASE CONFIGURATION
# =====================================================================

# Path to SQLite database (Chinook sample database)
# Will be auto-downloaded by setup.py if not present
DATABASE_PATH=./data/chinook.db

# =====================================================================
# SYSTEM SETTINGS
# =====================================================================

# Maximum self-correction retry attempts (1-5 recommended)
MAX_RETRIES=3

# Default row limit for queries (prevents accidental large result sets)
DEFAULT_LIMIT=100

# Enable verbose logging (true/false)
# Set to 'true' for debugging, 'false' for cleaner output
VERBOSE=false

# =====================================================================
# SAFETY SETTINGS (DO NOT CHANGE unless you understand the implications)
# =====================================================================

# These are hardcoded in config/settings.py for safety:
# - FORBIDDEN_KEYWORDS: DROP, DELETE, INSERT, UPDATE, ALTER, CREATE, TRUNCATE
# - MAX_RESULT_ROWS: 1000
# - All queries must include LIMIT clause
# - SELECT * is always rejected
